# マヨイビト DCG v0.7 ― Stage 4 チェックリスト

既存機能への接続（第27部 Stage 4）の完了確認です。仕様書 第28.4 の項目に沿っています。
自動テスト `tests/v07-bridge-tests.js`（30項目・全通過）で機械的に検証済みの項目には ✅ を付けています。
既存機能そのものの動作は実機での確認をお願いします（本体には手を入れていません）。

## 接続した導線

| v0.7 の入口 | つながる既存画面 | 戻り先タブ |
|---|---|---|
| カード → デッキ一覧 | `deck-list` | カード |
| カード → カード一覧 | `card-list` | カード |
| 対戦 → その他対戦 → トレーニング → CPU対戦 | `cpu-setup` | 対戦 |
| 対戦 → その他対戦 → トレーニング → ひとりまわし | `solo-setup` | 対戦 |
| 対戦 → その他対戦 → チュートリアル | `tutorial-select` | 対戦 |
| 対戦 → その他対戦 → 開発者用モード | `dev-mode` | 対戦 |
| その他 → 設定 → ヘルプ → ゲームの遊び方 | `howto` | その他 |
| その他 → 設定 → ヘルプ → チュートリアル | `tutorial-select` | その他 |
| プロフィール → お気に入りカード | 既存のカード拡大詳細を流用 | ― |

## 接続の確認（第28.4）

- ✅ デッキ一覧へ進める
- デッキ編集・保存が正常 … 実機確認（既存の保存処理には未変更）
- ✅ カード一覧へ進める
- カード一覧の既存機能が正常 … 実機確認（検索・絞り込み・詳細表示）
- ✅ CPU対戦へ進める
- CPU対戦が最後まで進行する … 実機確認（対戦ロジック・CPU AI は未変更）
- ✅ ひとりまわしへ進める
- ひとりまわしが正常 … 実機確認
- チュートリアル基本編が正常 … 実機確認（チュートリアル判定は未変更）
- チュートリアル応用編が正常 … 実機確認
- 開発者用モードが正常 … 実機確認（CPU観戦を含む）
- ✅ ゲームの遊び方へ進める
- ✅ エリーゼ詳細が開く（既存のカード拡大詳細を流用）
- ✅ カード詳細の外側タップで閉じない（既存仕様がそのまま該当）
- ✅ カード詳細の戻る（閉じる）でプロフィールへ戻る

## 戻る・履歴（第17部）

- ✅ 個別画面から入口タブへ戻る（既存メニューの最初の画面で戻る → v0.7 ハブ）
- ✅ ブラウザー戻るが画面内戻ると一致（既存内では1つ戻る／入口ではハブへ）
- ✅ メインハブの非ホームタブで戻るとホーム
- ✅ ホームで戻るとブラウザー標準
- ✅ メインタブ移動が履歴に大量追加されない（タブ切替は履歴を積まない）
- ✅ 対戦中の戻るで確認が出る（既存のリタイア確認に委ね、勝手に抜けさせない）
- ✅ 未保存デッキ編集の戻るで確認が出る（既存の `tryLeave` に委ねる）

## 安定性

- ✅ イベントリスナーが重複しない（ブリッジの初期化は1回だけ、`_inited` で保護）
- ✅ タイマーが重複しない（既存 `V7Timers` の二重防止をそのまま使用）
- ✅ コンソールに未処理エラーがない（boot〜各導線起動のスモークで例外ゼロ）

## 重点保護（第27部）― 触っていないもの

- 対戦ロジック（`game.js` 系）… 変更なし
- チュートリアル判定 … 変更なし
- デッキ保存 … 変更なし
- ドラッグ操作 … 変更なし
- CPU AI … 変更なし

既存ファイルへの変更は `js/preview.js` の2か所のみで、いずれも
「対戦・観戦から抜けたあと、どこへ戻るか」の分岐を足しただけです
（後片づけ処理そのものは既存のまま）。

## Stage 4 で「行わない」こと（仕様書どおり未実施）

- 既存画面の外装リフォーム（共通背景・共通ヘッダー適用・タップ領域改善など）
  → 第21部の内容は **Stage 5** の担当です。

## テスト結果

- 全35スイート：**失敗0**
- Stage 4 ブリッジテスト：**30/30 通過**
- Stage 3 個別画面テスト：**80/80 通過**（維持）
- ルール処理の指紋：`90b8f6dae89d6ece`（**変化なし**＝対戦ロジックに影響なし）
- 配布前点検（release-tests）：**29/29 通過**

## 実機で特に見ていただきたいところ

1. 各導線から既存機能へ入れるか（上の表の9本）
2. 既存機能から戻ったとき、入ったときのタブに戻るか
3. CPU対戦を最後まで進めて、終了後に対戦タブへ戻るか
4. デッキを編集して保存できるか／未保存で戻ろうとすると確認が出るか
5. チュートリアル（基本編・応用編）が最後まで正常に進むか
